<br />
<br />
<li>
  <%= @firstname.first.first_name %>
  <%= notification.event %>
  <span> <%= notification.created_at.in_time_zone('Eastern Time (US & Canada)').strftime ("%m/%d/%y at %l:%M %p") %></span>
</li>
