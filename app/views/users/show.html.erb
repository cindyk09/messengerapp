
 <h3>Inbox</h3>

  <div class="item">
    <% current_user.chats.each do |chat| %>
      <%= link_to chat.receiver.first_name, chat_messages_path(chat)%>
      <%= link_to "Delete", chat_path(chat), method: :delete%><br>
    <% end %>
  </div>
